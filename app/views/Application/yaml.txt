models.PlayVersion(p124):
    majorVersion: 1
    name: 1.2.4

models.PlayVersion(p20):
    majorVersion: 2
    name: 2.0


#{list modules, as:'module'}
models.Module(m${module_index}):
    moduleKey: ${module.name}
    moduleName: ${module.fullname}
    contributors: ${module.author}
    projectUrl: ${module.homepage}
    description: ${module.description}

#{list module.releases, as:'release'}
models.ModuleVersion(m${module_index}v${release_index}):
    module: m${module_index}
    compatibility: [ p124 ]
    versionCode: ${release.version}
    releaseDate: ${release.publishedDate.format('yyyy-MM-dd')}
#{if release.frameworkMatch}
    releaseNotes: Works with Play ${release.frameworkMatch}
#{/if}

#{/list}

#{/list}
